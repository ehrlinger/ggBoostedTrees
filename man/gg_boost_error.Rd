% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_boost_error.R
\name{gg_boost_error}
\alias{gg_boost_error}
\title{Boosting error trajectory data object}
\usage{
gg_boost_error(object, use.rmse = TRUE, ...)
}
\arguments{
\item{object}{A fitted \code{\link[boostmtree]{boostmtree}} object, or a
fitted \code{BoostMLR} object.}

\item{use.rmse}{Logical. When \code{TRUE} (default) return the standardized \code{l2}
error; when \code{FALSE} return the unstandardized squared error.}

\item{...}{Not used; present for S3 consistency.}
}
\value{
A \code{gg_boost_error} \code{data.frame} with columns:
\describe{
\item{iteration}{Integer boosting iteration, from 1.}
\item{value}{Numeric error at that iteration.}
\item{response}{Factor naming the response.}
\item{optimal}{Logical, \code{TRUE} at the cross-validated optimal iteration.}
}
}
\description{
Extract the error path of a boosted multivariate tree fit as a function of
the boosting iteration, together with the optimal iteration selected by
cross-validation.
}
\details{
\strong{The fit must have been grown with \code{cv.flag = TRUE}.} \code{boostmtree}
records \code{err.rate} and \code{m.opt} only when cross-validation ran; a default fit
carries neither, and there is no error path to extract. If this function
reports that the fit has no error path, refit with \code{cv.flag = TRUE}.

\code{boostmtree} stores the error on the standardized response scale in the
\code{l2} column. \code{use.rmse = FALSE} returns \code{(l2 * y.sd)^2}, the squared error
on the original scale.

This figure also accepts a \code{BoostMLR} \emph{grow} fit (predict
objects are not supported -- see below). \code{BoostMLR} records \code{Error_Rate}
as an M-by-response matrix already on a single scale, so there is no
\code{use.rmse} argument for that backend; passing one is refused. \code{BoostMLR}
grow objects also select no optimal iteration anywhere in the object --
\code{partial.BoostMLR()} takes \code{Mopt} as a user-supplied argument instead --
so \code{optimal} is \code{FALSE} for every row; deriving an argmin here would
report a choice the backend never made.

A \code{BoostMLR} \strong{predict} object is refused outright, even though it
shares the \code{"BoostMLR"} class and also carries an \code{Error_Rate}. Its
\code{Error_Rate} is test error rather than the training path documented above,
and it records a real \code{Mopt} that this extractor would otherwise silently
discard and overwrite with \code{optimal = FALSE}.
}
\examples{
\donttest{
sim <- boostmtree::simLong(n = 25, n.time = 4, model = 1)$data.list
fit <- boostmtree::boostmtree(
  x = sim$features, tm = sim$time, id = sim$id, y = sim$y,
  M = 50, cv.flag = TRUE, verbose = FALSE
)
gg_dta <- gg_boost_error(fit)
plot(gg_dta)
}

}
\seealso{
\code{\link{plot.gg_boost_error}}, \code{\link{gg_boost_path}}
}
