% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_boost_vimp.R
\name{gg_boost_vimp}
\alias{gg_boost_vimp}
\title{Variable importance data object}
\usage{
gg_boost_vimp(object, components = c("main", "interaction"), ...)
}
\arguments{
\item{object}{A \code{\link[boostmtree]{vimp.boostmtree}} object.}

\item{components}{Character vector naming the components to extract, any of
\code{"main"} and \code{"interaction"}. Defaults to both.}

\item{...}{Not used; present for S3 consistency.}
}
\value{
A \code{gg_boost_vimp} \code{data.frame} with columns:
\describe{
\item{variable}{Factor covariate name.}
\item{importance}{Numeric importance.}
\item{component}{Factor, \code{main} or \code{interaction}.}
\item{response}{Factor naming the response.}
}
with attributes \code{metric} (the importance metric) and \code{time.effect}.
}
\description{
Extract variable importance from a \code{\link[boostmtree]{vimp.boostmtree}}
object, for both the main effect of each covariate and its interaction with
time.
}
\details{
\code{boostmtree} reports importance in two parts. The main effect asks what is
lost by perturbing a covariate; the interaction asks what is lost by
perturbing its interaction with time. For a longitudinal model the second is
often the interesting one, since it is where a covariate whose effect
\emph{changes} over follow-up shows up.

\code{boostmtree} labels the interaction rows \code{x1:time}, \code{x2:time} and so on. The
suffix is stripped here, so a variable carries one label across both
components and can be compared or dodged against itself.

The importance metric is a string on the source object rather than a fixed
quantity, so it travels as the \code{metric} attribute and the renderer uses it to
label the axis. The overall time effect has no variable to attach to and
travels as the \code{time.effect} attribute.

A joint importance object (\code{vimp.boostmtree(fit, joint = TRUE)}) reports a
single combined value, labelled \code{joint.vimp}. Note that CRAN \code{boostmtree}
2.0.0 cannot produce one at all; the patched build this package requires is
needed to compute it.
}
\examples{
\donttest{
sim <- boostmtree::simLong(n = 25, n.time = 4, model = 1)$data.list
fit <- boostmtree::boostmtree(
  x = sim$features, tm = sim$time, id = sim$id, y = sim$y,
  M = 50, cv.flag = TRUE, verbose = FALSE
)
plot(gg_boost_vimp(boostmtree::vimp.boostmtree(fit)))
}

}
\seealso{
\code{\link{plot.gg_boost_vimp}}, \code{\link{gg_boost_effect}}
}
