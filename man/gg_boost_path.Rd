% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_boost_path.R
\name{gg_boost_path}
\alias{gg_boost_path}
\title{Boosting parameter path data object}
\usage{
gg_boost_path(object, parameters = c("rho", "phi", "lambda"), ...)
}
\arguments{
\item{object}{A fitted \code{\link[boostmtree]{boostmtree}} object.}

\item{parameters}{Character vector naming the paths to extract, any of
\code{"rho"}, \code{"phi"}, and \code{"lambda"}. Defaults to all three.}

\item{...}{Not used; present for S3 consistency.}
}
\value{
A \code{gg_boost_path} \code{data.frame} with columns:
\describe{
\item{iteration}{Integer boosting iteration, from 1.}
\item{value}{Numeric parameter value at that iteration.}
\item{parameter}{Factor naming the parameter.}
\item{response}{Factor naming the response.}
}
}
\description{
Extract the estimated correlation, variance, and penalized-spline smoothing
parameters of a boosted multivariate tree fit as functions of the boosting
iteration.
}
\details{
\code{boostmtree} re-estimates three quantities at every boosting iteration:
\code{rho}, the within-subject correlation; \code{phi}, the variance component; and
\code{lambda}, the P-spline smoothing parameter for the time-covariate
interaction. Their paths diagnose whether the variance structure settled or
is still drifting when boosting stops.

Unlike the error path, these are recorded on every fit and do not require
\code{cv.flag = TRUE}. A parameter absent from the fit is dropped silently; it is
an error only when none of the requested parameters is present.
}
\examples{
\donttest{
sim <- boostmtree::simLong(n = 25, n.time = 4, model = 1)$data.list
fit <- boostmtree::boostmtree(
  x = sim$features, tm = sim$time, id = sim$id, y = sim$y,
  M = 50, cv.flag = TRUE, verbose = FALSE
)
plot(gg_boost_path(fit))
}

}
\seealso{
\code{\link{plot.gg_boost_path}}, \code{\link{gg_boost_error}}
}
